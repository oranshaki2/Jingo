import { View, Text } from "react-native";
export default function Learn() {
  return (
    <View style={{ padding: 24 }}>
      <Text style={{ fontSize: 24 }}>Learn from Song</Text>
      {/* TODO: audio player + synced lyrics */}
    </View>
  );
}